#===============================================================================
# OJS Docker Environment Configuration
#===============================================================================

# --- OJS Image Settings ---
OJS_IMAGE=ojs
OJS_VERSION=3_5_0-3

# --- Database Configuration ---
# DB_HOST must match the database service name in compose.yml
DB_HOST={{DB_HOST}}
DB_NAME=ojs
DB_USER=ojs
# SECURITY: Change these passwords before deploying to production!
DB_PASSWORD=ojs
DB_ROOT_PASSWORD=ojs

# --- Traefik / SSL Settings ---
# Domain for Traefik routing (used in docker-compose labels)
DOMAIN={{DOMAIN}}
# Email for Let's Encrypt certificate notifications
ACME_EMAIL={{ACME_EMAIL}}

# --- OJS Application Settings ---
# {{DOMAIN}} placeholder is replaced by deploy.sh
OJS_URL=https://{{DOMAIN}}
OJS_BASE_URL=https://{{DOMAIN}}
OJS_BASE_URL_INDEX=https://{{DOMAIN}}/index
TRUSTED_HOSTS={{DOMAIN}},localhost,127.0.0.1

# --- Optional: Timezone ---
# TZ=Asia/Baghdad
